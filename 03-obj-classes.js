class WhataNiceClass {
    // class methods
    coolMethod() { .. }   // pas de virgule!
    boringMethod() { .. }
}

var WhatisThat = {
    // object methods
    coolMethod() { .. },   // virgule
    boringMethod() { .. },

    // (anonymous) function expression property
    oldSchool: function() { .. }
};